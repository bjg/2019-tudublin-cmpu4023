endpoint: http://localhost:4466
datamodel: datamodel.prisma
generate:
  - generator: javascript-client
    output: ./generated/prisma-client/
hooks:
  post-deploy:
    - prisma generate
    - graphql get-schema --endpoint http://localhost:4466 --output schema.graphql
    - graphql prepare
